## 3.1 Section Code

### Lab - Microsoft Fabric Data Warehouse - Ingesting Data - COPY Command

```sql
-- 1. First, create the target table.
CREATE TABLE [SalesOrderHeader] (
    [SalesOrderID] [int] NULL,
    [OrderDate] [date] NULL,
    [Status] [smallint] NULL,
    [SalesOrderNumber] [varchar](25) NULL,
    [CustomerID] [int] NULL,
    [SalesPersonID] [int] NULL,
    [TerritoryID] [int] NULL,
    [SubTotal] [float] NULL,
    [TaxAmt] [float] NULL,
    [Freight] [float] NULL,
    [TotalDue] [float] NULL
);

-- 2. For information about the data types supported by Microsoft Fabric:
-- https://learn.microsoft.com/en-us/fabric/data-warehouse/data-types

-- 3. Copy the data from the CSV file into the target table.
COPY INTO [SalesOrderHeader]
FROM 'https://appstore5545464.blob.core.windows.net/raw/SalesOrderHeader.csv'
WITH (
    FILE_TYPE = 'CSV',
    FIRSTROW = 2
);
```

### Lab - Microsoft Fabric Data Warehouse - Ingesting Data - COPY Command - SAS

```sql
-- 1. First, delete the existing data from the table.
DELETE FROM [SalesOrderHeader];

-- 2. Copy the data from the CSV file into the table
--    using a Shared Access Signature (SAS) for authentication.
COPY INTO [SalesOrderHeader]
FROM 'https://appstore5545464.blob.core.windows.net/raw/SalesOrderHeader.csv'
WITH (
    FILE_TYPE = 'CSV',
    CREDENTIAL = (
        IDENTITY = 'Shared Access Signature',
        SECRET = 'sv=2024-11-04&ss=b&srt=sco&sp=rl&se=2025-03-21T16:40:02Z&st=2025-03-21T08:40:02Z&spr=https&sig=fw%2BVEPOJp4hnJZ7HjEAmcUxmCfh6FST40BqLJDzQpMw%3D'
    ),
    FIRSTROW = 2
);
```

> **Note:** A real SAS token should be treated as a secret. Avoid storing or sharing active SAS tokens in notes, source control, or documentation.

### Lab - Microsoft Fabric Data Warehouse - Cloning Tables

```sql
-- 1. First, create a new schema for the staging tables.
CREATE SCHEMA staging;

-- 2. Create a clone of the existing SalesOrderHeader table
--    in the staging schema.
CREATE TABLE staging.SalesOrderHeader
AS CLONE OF dbo.SalesOrderHeader;

-- 3. Update a value in the cloned table.
UPDATE staging.SalesOrderHeader
SET Status = 6
WHERE SalesOrderID = 47350;
```

### Lab - Microsoft Fabric Warehouse - CREATE TABLE AS SELECT

```sql
-- 1. First, drop the existing staging table.
DROP TABLE staging.SalesOrderHeader;

-- 2. Create a new table using the results of a SELECT statement.
CREATE TABLE staging.SalesOrderHeader
AS
SELECT *
FROM dbo.SalesOrderHeader;
```

### Lab - Microsoft Fabric Data Warehouse - Building Our Dimension Tables

```sql
-- 1. Create the dimension tables.

CREATE TABLE [DimProduct] (
    [ProductID] [int] NULL,
    [ProductName] [varchar](50) NULL,
    [MakeFlag] [bit] NULL,
    [Color] [varchar](15) NULL,
    [SafetyStockLevel] [smallint] NULL,
    [ProductModelID] [int] NULL,
    [ProductModelName] [varchar](500) NULL
);

CREATE TABLE [DimCustomer] (
    [CustomerID] [int] NULL,
    [StoreID] [int] NULL,
    [StoreName] [varchar](500) NULL
);
```

### Lab - Microsoft Fabric Data Warehouse - Building the Dimension Date Table

```sql
-- 1. First, create the date dimension table.
CREATE TABLE [DimDate] (
    [OrderDate] [date] NULL,
    [DateYear] [varchar](6) NULL,
    [DateMonth] [varchar](12) NULL,
    [DateMonthNumber] [varchar](6) NULL
);

-- 2. Create a stored procedure that populates the date dimension table.
CREATE PROC AddDate
AS
BEGIN
    DECLARE @StartDate date;
    DECLARE @EndDate date;

    SET @StartDate = '2011-05-31';
    SET @EndDate = '2014-06-30';

    WHILE @StartDate <= @EndDate
    BEGIN
        INSERT INTO DimDate
        SELECT
            @StartDate,
            DATEPART(yyyy, @StartDate),
            DATENAME(MONTH, DATEADD(MONTH, DATEPART(mm, @StartDate), -1)),
            DATEPART(mm, @StartDate);

        SET @StartDate = DATEADD(day, 1, @StartDate);
    END
END;

-- 3. Execute the stored procedure to populate the table.
EXEC AddDate;
```

### Lab - Microsoft Fabric Data Warehouse - SQL - Basics

```sql
-- 1. Retrieve all rows and columns from the FactSales table.
SELECT *
FROM FactSales;

-- 2. Count the total number of rows in the FactSales table.
SELECT COUNT(*)
FROM FactSales;

-- 3. Retrieve orders placed between 2011 and 2012.
SELECT
    OrderDate,
    SalesOrderID,
    OrderQty,
    TotalAmt
FROM FactSales
WHERE YEAR(OrderDate) BETWEEN 2011 AND 2012
ORDER BY OrderDate;
```

### Lab - Microsoft Fabric Data Warehouse - SQL - GROUP BY

```sql
-- 1. Count the number of sales for each product.
SELECT
    ProductID,
    COUNT(SalesOrderID) AS SalesCount
FROM FactSales
GROUP BY ProductID
ORDER BY SalesCount;

-- 2. Filter the grouped results to return only products
--    with more than 100 sales.
SELECT
    ProductID,
    COUNT(SalesOrderID) AS SalesCount
FROM FactSales
GROUP BY ProductID
HAVING COUNT(SalesOrderID) > 100
ORDER BY SalesCount;

-- 3. Combine WHERE, GROUP BY, and HAVING to find products
--    with more than 100 sales during 2012.
SELECT
    ProductID,
    COUNT(SalesOrderID) AS SalesCount
FROM FactSales
WHERE YEAR(OrderDate) = 2012
GROUP BY ProductID
HAVING COUNT(SalesOrderID) > 100
ORDER BY COUNT(SalesOrderID);
```

### Lab - Microsoft Fabric Data Warehouse - SQL - Finding Duplicate Values

```sql
-- 1. Find SalesOrderID values that appear more than once.
SELECT
    SalesOrderID,
    COUNT(SalesOrderID) AS SalesCount
FROM FactSales
GROUP BY SalesOrderID
HAVING COUNT(SalesOrderID) > 1;
```

### Lab - Microsoft Fabric Data Warehouse - SQL - JOIN

```sql
-- 1. Join FactSales with DimProduct to retrieve the product model name
--    and count the number of sales for each product model.
SELECT
    dt.ProductModelName,
    COUNT(SalesOrderID) AS SalesCount
FROM FactSales AS fs
JOIN DimProduct AS dt
    ON fs.ProductID = dt.ProductID
GROUP BY dt.ProductModelName;
```

### Lab - Microsoft Fabric Data Warehouse - SQL - User-Defined Function

```sql
-- 1. Create a table-valued function that returns products
--    for a specified ProductModelID.
CREATE FUNCTION GetProducts(@ProductModelID INT)
RETURNS TABLE
AS
RETURN (
    SELECT
        ProductID,
        ProductName,
        ProductModelName
    FROM DimProduct
    WHERE ProductModelID = @ProductModelID
);

-- 2. Create a view that joins FactSales with DimProduct.
CREATE VIEW FactSalesDimProductvw
AS
SELECT
    fs.SalesOrderID,
    fs.OrderDate,
    fs.TotalAmt,
    fs.OrderQty,
    fs.ProductID,
    dt.ProductModelID
FROM FactSales AS fs
JOIN DimProduct AS dt
    ON fs.ProductID = dt.ProductID;

-- 3. Use OUTER APPLY to execute the function for each row
--    returned by the view.
SELECT *
FROM FactSalesDimProductvw
OUTER APPLY GetProducts(ProductModelID);
```

## 3.2 What is Microsoft Fabric

Azure vs Fabric

**Fabric** is more focus on analytics and data warehousing, while Azure is a broader cloud platform that includes a wide range of services for computing, storage, networking, and more. Fabric provides a unified experience for data engineers and analysts to ingest, prepare, and analyze data in a collaborative environment.

**Azure** is a cloud computing platform and service created by Microsoft for building, testing, deploying, and managing applications and services through Microsoft-managed data centers. It provides a wide range of cloud services, including those for computing, analytics, storage, and networking.

Fabric inlcude Power BI, Data Factory, Synapse, and Data Lake, which are all integrated into a single platform for data analytics and business intelligence. Azure provides a broader set of services that can be used for a variety of purposes beyond data analytics, such as virtual machines, databases, AI services, and more.

**OneLake** is a unified data lake that allows organizations to store and manage all their data in a single location. It provides a scalable and secure environment for storing structured, semi-structured, and unstructured data. OneLake is designed to work seamlessly with Microsoft Fabric, enabling users to easily ingest, prepare, and analyze data from various sources.

## 3.3 Microsoft Fabric Terms

### 3.3.1 Fabric Terms

**Experience**: A collection of related services and tools that work together to provide a specific functionality or capability within Microsoft Fabric. Examples include the Data Engineering experience, the Data Science experience, and the Business Intelligence experience.

**Workspace**: A collaborative environment within Microsoft Fabric where users can create, manage, and share data projects, datasets, reports, and dashboards. Workspaces provide a centralized location for teams to work together on data-related tasks.

**Item**: A specific object or resource within a workspace, such as a dataset, report, dashboard, or dataflow. Items can be created, modified, and shared with other users within the workspace.

**Capacity**: A measure of the resources allocated to a workspace or experience within Microsoft Fabric. Capacity determines the amount of storage, processing power, and other resources available for data projects and tasks. Capacities can be assigned to workspaces to ensure that they have the necessary resources to perform their functions effectively.

### 3.3.2 Data Engineering

**Fabric Data Warehouse**: A cloud-based data storage and analytics solution within Microsoft Fabric that allows users to store, manage, and analyze large volumes of structured and semi-structured data. The Fabric Data Warehouse provides a scalable and secure environment for data warehousing, enabling users to perform complex queries, aggregations, and transformations on their data.

**T-SQL**: A variant of the SQL (Structured Query Language) used in Microsoft Fabric for querying and managing data within the Fabric Data Warehouse. T-SQL includes additional features and extensions specific to Microsoft Fabric, allowing users to perform advanced data manipulation and analysis tasks.

**Lakehouse**: A data architecture that combines the benefits of data lakes and data warehouses, allowing organizations to store and analyze both structured and unstructured data in a single platform. The Lakehouse architecture enables users to perform advanced analytics, machine learning, and business intelligence tasks on their data.

**Notebook**: An interactive environment within Microsoft Fabric where users can create, edit, and run code, visualizations, and analyses. Notebooks support multiple programming languages, such as Python, R, and SQL, allowing users to perform data exploration, analysis, and visualization tasks in a flexible and collaborative manner.

### 3.3.3 Data Factory

**Connector**: A pre-built integration within Microsoft Fabric that allows users to connect to various data sources, such as databases, APIs, and cloud services. Connectors enable users to easily ingest and access data from different systems, facilitating data integration and analysis tasks.

**Data pipeline**: A series of data processing steps within Microsoft Fabric that define the flow of data from source to destination. Data pipelines can include data ingestion, transformation, and loading tasks, allowing users to automate and orchestrate their data workflows.

**Dataflow Gen2**: An enhanced version of dataflows within Microsoft Fabric that provides improved performance, scalability, and functionality for data transformation and preparation tasks. Dataflow Gen2 allows users to create reusable data transformation logic, enabling efficient data processing and analysis.

### 3.3.4 Real Time

**Eventhouse**: A real-time data processing and analytics service within Microsoft Fabric that allows users to ingest, process, and analyze streaming data from various sources. Eventhouse enables organizations to gain insights from real-time data, supporting use cases such as monitoring, alerting, and predictive analytics.

**Eventstream**: A continuous flow of data generated by events or actions within a system, application, or device. Eventstreams can be ingested and processed in real-time using Microsoft Fabric's Eventhouse service, enabling organizations to analyze and respond to events as they occur.

**KQL Database**: A database within Microsoft Fabric that supports querying and analyzing data using Kusto Query Language (KQL). KQL is a powerful query language designed for real-time data exploration and analysis, allowing users to perform complex queries, aggregations, and visualizations on their data.

## 3.4 Note on Microsoft Fabric Licensing

We need fabric capacity + storage for our workspace.

- Capacity
  - Option: pay as you go, or reserved capacity
  - SKU, CU (Capacity Unit) -> CU is a unit of measure for the resources allocated to a workspace or experience within Microsoft Fabric. It represents the amount of processing power, memory, and storage available for data projects and tasks. The number of CUs assigned to a workspace determines its performance and scalability.
  - Free trial: F64
- Storage (OneLake Storage)
  - Free trials: 1TB

## 3.5 On-boarding Ourselves onto Microsoft Fabric

Azure - Create a new user - This user to get Microsoft Fabric trial capacity. The user will be the admin of the workspace.

app.fabric.microsoft.com - Use the new user to log in and create a new workspace.

## 3.6 Getting Microsoft Fabric Trial Capacity

Create a new workspace in Microsoft Fabric. The workspace will be used to create and manage data projects, datasets, reports, and dashboards. Assign the trial capacity to the workspace to enable access to the necessary resources for data analytics and business intelligence tasks.

- Data Engineering
- Data Factory
- Data Science
- Data Warehouse
- etc

Workspace settings - Need compute capacity for running workloads - Edit: Trial; Select License

Profile -> Free trial -> Activate

Settings -> Admin portal -> Capacity settings -> Trial: F64 (Trial)

## 3.7 Lab - Publishing a Report from Power BI Desktop to Fabric

## 3.8 Review of the Data Warehousing Fundamentals

## 3.9 Lab - Microsoft Fabric - Creating a Sample Data Warehouse

## 3.10 Microsoft Fabric - Ingesting Data

## 3.11 Lab - Creating an Azure Storage Account

## 3.12 Lab - Microsoft Fabric Data Warehouse - Ingesting Data - COPY Command

## 3.13 Lab - Microsoft Fabric Data Warehouse - Ingesting Data - COPY Command - SAS

## 3.14 Lab - Microsoft Fabric Data Warehouse - Visual Query

## 3.15 Lab - Microsoft Fabric Data Warehouse - Cloning Tables

## 3.16 Lab - Microsoft Fabric Warehouse - CREATE TABLE AS SELECT

## 3.17 Lab - Microsoft Fabric - Ingesting Data - Data Pipeline

## 3.18 Lab - Microsoft Fabric - Ingesting Data - Data Flow Gen2

## 3.19 Lab - Microsoft Fabric Data Warehouse - Building Our Fact Table

## 3.20 Lab - Microsoft Fabric Data Warehouse - Building Our Dimension Tables

## 3.21 Lab - Microsoft Fabric Data Warehouse - Building the Dimension Date Table

## 3.22 Lab - Microsoft Fabric Data Warehouse - Default Semantic Model

## 3.23 Lab - Microsoft Fabric Data Warehouse - Column Profiling

## 3.24 Data Warehouse - Slowly Changing Dimensions

## 3.25 Microsoft Fabric - Power Query - Query Folding

## 3.26 Lab - Microsoft Fabric Data Warehouse - SQL - Basics

## 3.27 Lab - Microsoft Fabric Data Warehouse - SQL - Group By

## 3.28 Lab - Microsoft Fabric Data Warehouse - SQL - Finding Duplicate Values

## 3.29 Lab - Microsoft Fabric Data Warehouse - SQL - JOIN

## 3.30 Lab - Microsoft Fabric Data Warehouse - SQL User-Defined Function

## 3.31 Quiz 2: Section Quiz
